class R {
    public static void main(String[] args) {
        try {

            
        } catch(ArithmeticException e) {


        }
        // => Essential Code
        //If we write it outside the try-catch
        // and if some exception occured inside try for which there is no suitable catch block
        //then again this essential code will not be executed
        //Hence we cannot write it here too
    }
}