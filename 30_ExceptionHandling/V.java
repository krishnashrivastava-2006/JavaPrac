class V {
    public static void main(String[] args) {
        try {
            //problem generating code
            //code dependent on problem generating code
        } catch(ArithmeticException e) {
            //solution code for problem
        } finally {
            //Essential Code
            //must run even if exception occur or not occur
            //even if solution exists or not exists
            //it will compulsory run if execution control has enetered the try block
        }
    }
}